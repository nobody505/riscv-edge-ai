# K1 Runtime Archives

从 K1 RISC-V 环境导出的 Python 3.12 与 SpaceMIT ONNX Runtime 依赖，用于在同型号新板上恢复兼容运行环境。

安装前必须在本目录执行：

```bash
sha256sum -c SHA256SUMS
```

这些归档与 RISC-V 64、Python 3.12 和目标系统 ABI 绑定，不适用于 x86_64 主机。
